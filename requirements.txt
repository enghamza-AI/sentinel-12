# Sentinel-12: requirements.txt
# ================================
# Install all dependencies with:
#   pip install -r requirements.txt
#
# These are the only non-stdlib packages this project uses.
# Pinned to minor versions (e.g. ">=1.3") rather than exact
# patches so `pip install` picks a compatible recent version
# without being unnecessarily strict.

numpy>=1.24          # array math, random number generation
pandas>=2.0          # CSV I/O and DataFrame operations
scikit-learn>=1.3    # model, scaler, metrics (confusion matrix etc.)
matplotlib>=3.7      # plotting: confusion matrix heatmap, curves
joblib>=1.3          # saving/loading the trained model to disk
